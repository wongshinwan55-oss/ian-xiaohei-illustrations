替换小黑——补丁计划

下面是我准备为你运行的自动化替换计划：

目标
- 在 replace-xiaohei-with-custom-ip 分支上查找所有字符串“小黑”的出现位置，并把它们按规则替换或注释保留为“历史示例”。

规则（安全策略）
1）文档/参考文件（.md）：
   - 如果文本为说明/引用性描述（比如提到历史上的 "小黑"） -> 在该处加注释并保留原文：
     例如：`（历史示例：原文使用“小黑”）`。
   - 如果文本为操作性 prompt 或生成模板（会被 agent 直接使用） -> 直接替换为 `{IP=cat}` / `{IP=duck}` / `{IP=fusion}` 并确保变量说明在文档顶部存在。

2）代码/配置文件（.yaml / .json / .py）：
   - 对于 key/field 的默认值或示例值，直接替换为 `duck`/`cat`/`fusion`。保留注释描述。

3）示例图片/文件名：
   - 不改动二进制文件名（以免破坏历史引用），但在引用该图片的文档中把说明替换为新 IP，并在图片附近注释“示例图片保留原样以便参考”。

4）Search case-insensitive: 小黑, 小黑。匹配中文全字。

将要生成的输出
- 一个清单（CSV/Markdown）列出所有出现位置：文件路径、行号、上下文片段、建议动作（替换/注释/跳过）
- 一个 patch（或一组 patch）用于自动应用已建议的替换（并包含 commit message）

我将如何执行
1. 在你的分支上运行全文搜索并收集结果（注意：如果出现大量结果我会把最关键的文档先行列出）
2. 生成替换补丁并把 diff 发给你（99% 的时候我会先让你审阅）
3. 在你确认后把补丁 apply 并 push 到 replace-xiaohei-with-custom-ip（一次性 commit）

需要你确认
- 请回复 “确认执行替换计划”，我就开始跑搜索并把初步清单发给你。